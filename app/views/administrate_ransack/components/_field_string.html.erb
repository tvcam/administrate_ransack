<% field_key = model.ransackable_scopes.include?(field) ? field : "#{field}_cont" %>

<div class="row mb-3">
  <%= form.label(label, class: 'pl-0 form-label') %>
  <%= form.search_field(field_key, class: 'form-control form-control-sm') %>
</div>
